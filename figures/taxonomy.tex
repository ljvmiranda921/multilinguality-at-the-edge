% figures/taxonomy.tex
% Taxonomy diagram with aligned tiers and forked edges
% Usage: \input{figures/taxonomy.tex}

\scalebox{0.80}{%
  \begin{forest}
    forked edges,
    for tree={
    grow'=0,
    draw,
    rounded corners,
    minimum height=1em,
    minimum width=5em,
    node options={align=center},
    where level>=1{node options={align=left}}{},
    anchor=west,
    parent anchor=east,
    child anchor=west,
    l sep=2em,
    s sep=0.5em,
    tier/.wrap pgfmath arg={tier #1}{level()},
    edge={-latex},
    },
    %\begin{noindent}
  [Resource\\Constraints, fill=white, minimum width=8em, name=tier1
    [\textbf{Data} (\S\ref{sec:access_to_high_quality_data}), fill=clightblue, for descendants={fill=clightblue}, name=tier2
      [Training (\S\ref{sec:training}), name=tier3
        [Data Curation, name=tier4] % filtering, deduplication, quality control
        [Data Scaling] % multilingual corpora, web crawling, synthetic data generation
        [Cross-lingual Transfer] % translate-train, zero-shot transfer, multilingual pretraining
      ]
      [Evaluation (\S\ref{sec:evaluation})
        [Localized Benchmarks] % community-focused benchmarks, culturally-aligned tasks
        [Evaluation Methods] % HITL, LLM-as-judge, automatic metrics for low-resource settings
      ]
    ]
    [\textbf{Infrastructure} (\S\ref{sec:access_to_infrastructure}), fill=clightcrest, for descendants={fill=clightcrest}
      [Hardware (\S\ref{sec:hardware})
        [Smaller LLMs] % quantization, pruning, knowledge distillation
        [On-Device ML] % mobile deployment, edge inference, TinyML
        [Efficient Architectures \& Adaptation] % MoE, sparse models, linear attention
        % [Efficient Finetuning] % adapters, LoRA, parameter-efficient methods
      ]
      [Network (\S\ref{sec:network})
        [Offline-First Design] % offline inference, edge caching, local models
        [API Optimization] % batching, request caching, latency reduction
      ]
      [Privacy (\S\ref{sec:privacy})
        [Privacy-preserving Training] % federated learning, differential privacy
        [Privacy-preserving Inference] % cryptographic methods, homomorphic encryption
      ]
    ]
    [\textbf{Readiness} (\S\ref{sec:access_to_operational_readiness}), fill=clightpurple, for descendants={fill=clightpurple}
      [Expertise (\S\ref{sec:expertise})
        [Community-led Development] % grassroots communities, sovereign LLM efforts
        [Capacity Building] % training programs, workshops, educational resources
      ]
      % [Tooling (\S\ref{sec:tooling})
      %   [Open-source Tooling] % annotation tools, no-code platforms, deployment guides
      % ]
    ]
  ]
  %\end{noindent}
    % Tier annotations above each tier
    \node[anchor=south, font=\itshape, text=gray] at ([yshift=1.7em]tier2.north) {Constraint (\S\ref{sec:binding_constraint})};
    \node[anchor=south, font=\itshape, text=gray] at ([yshift=1.4em]tier3.north) {Sub-constraint};
    \node[anchor=south, font=\itshape, text=gray] at ([yshift=0.5em]tier4.north) {Intervention (\S\ref{sec:survey_of_interventions})};
  \end{forest}%
}